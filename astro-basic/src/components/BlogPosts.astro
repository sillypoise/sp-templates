---
// fetch posts from json placeholder
const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const posts = await response.json();
const postsToShow = posts.slice(0, 5);
---

<div class="bg-white py-24 sm:py-32">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:mx-0">
      <a
        href="/"
        class="my-8 inline-block rounded-lg bg-pretty px-3.5 py-1.5 text-base font-semibold shadow-sm ring-1 ring-gray-900/10 hover:ring-gray-900/20"
      >
        Home <span aria-hidden="true">→</span>
      </a>
      <h2
        class="mt-2 text-5xl font-semibold tracking-tight text-gray-900 sm:text-7xl"
      >
        Blog Posts
      </h2>
      <p
        class="mt-8 text-lg font-medium text-pretty text-gray-500 sm:text-xl/8"
      >
        Anim aute id magna aliqua ad ad non deserunt sunt. Qui irure qui lorem
        cupidatat commodo. Elit sunt amet fugiat veniam occaecat fugiat.
      </p>
    </div>
    <ul
      role="list"
      class="mx-auto mt-20 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3"
    >
      {
        postsToShow.map((post) => (
          <li>
            <a href={`/blog/${post.id}`}>
              <h3 class="mt-6 text-lg/8 font-semibold tracking-tight text-gray-900">
                {post.title}
              </h3>
              <p class="text-base/7 text-gray-600">{post.body}</p>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>
